{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "comments": {
      "$comicDate": {
        ".indexOn": ["timestamp"]
      }
    },
    "favorites": {
      "$userId": {
        ".indexOn": ["date"]
      }
    }
  }
}
